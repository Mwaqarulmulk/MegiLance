import AdminFeedbackPage from './AdminFeedbackPage';

export const metadata = { title: 'User Feedback & Reports — Admin — MegiLance' };

export default function Page() {
  return <AdminFeedbackPage />;
}
