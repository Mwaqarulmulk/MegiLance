# MegiLance Production Environment Template
# For Oracle Autonomous Database deployment via docker-compose.oracle.yml
# 
# ⚠️ CRITICAL: This file is a TEMPLATE. 
# - Copy to .env.production and fill with REAL production secrets.
# - NEVER commit .env.production with secrets to version control.
# - Store real secrets in OCI Vault, AWS Secrets Manager, or CI/CD secrets.
# - Rotate secrets regularly.

# === Oracle Database Connection ===
# Format: oracle+oracledb://USERNAME:PASSWORD@HOST:PORT/SERVICE_NAME?wallet_location=/path&wallet_password=PASS
# Replace <USERNAME>, <PASSWORD>, <SERVICE_NAME>, <WALLET_PASSWORD> with real values.
DATABASE_URL=oracle+oracledb://<USERNAME>:<PASSWORD>@<SERVICE_NAME>?wallet_location=/app/oracle-wallet&wallet_password=<WALLET_PASSWORD>

# === Oracle Cloud Infrastructure (OCI) ===
# These identifiers are public (not secret) but should be managed in source control carefully.
OCI_REGION=<your-region>
OCI_NAMESPACE=<your-namespace>
OCI_BUCKET_NAME=megilance-storage
OCI_COMPARTMENT_ID=<your-compartment-ocid>

# === Oracle Wallet Password ===
# Password to unlock the wallet file (sensitive — use secrets manager).
ORACLE_WALLET_PASSWORD=<your-wallet-password>

# === JWT & Security ===
SECRET_KEY=<generate-a-long-random-string-min-32-chars>
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# === Application Environment ===
ENVIRONMENT=production
DEBUG=False
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# === Optional: Email (SES, SendGrid, etc.) ===
# SMTP_HOST=<smtp-host>
# SMTP_PORT=587
# SMTP_USERNAME=<smtp-user>
# SMTP_PASSWORD=<smtp-password>
# EMAIL_FROM=noreply@yourdomain.com

# === Optional: Observability & Monitoring ===
# SENTRY_DSN=<sentry-dsn>
# LOG_LEVEL=INFO
